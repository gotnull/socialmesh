#!/usr/bin/env python3
"""Generate tak_server_test.dart — unit tests for TakServer."""
import os

OUT = "/Users/fulvio/development/socialmesh/test/services/tak/tak_server_test.dart"
os.makedirs(os.path.dirname(OUT), exist_ok=True)
lines = []
a = lines.append

a("// SPDX-License-Identifier: GPL-3.0-or-later")
a("// SPDX-FileCopyrightText: 2025-2026 gotnull (developer@socialmesh.app)")
a("import 'package:flutter_test/flutter_test.dart';")
a("import 'package:socialmesh/services/tak/tak_client_session.dart';")
a("import 'package:socialmesh/services/tak/tak_server.dart';")
a("")
a("void main() {")
a("  group('TakServer', () {")
a("    // Note: Full TLS server tests require actual certificates and sockets.")
a("    // These tests verify the public API surface and types.")
a("")
a("    test('TakServerEvent sealed class hierarchy', () {")
a("      const event1 = TakServerError('test');")
a("      expect(event1.message, 'test');")
a("      expect(event1, isA<TakServerEvent>());")
a("    });")
a("")
a("    test('TakServer constants', () {")
a("      expect(TakServer.defaultPort, 8089);")
a("      expect(TakServer.maxClients, 5);")
a("      expect(TakServer.inactivityTimeout, const Duration(seconds: 120));")
a("    });")
a("")
a("    test('TakSessionState enum values', () {")
a("      expect(TakSessionState.values, hasLength(3));")
a("      expect(TakSessionState.pendingNegotiation, isNotNull);")
a("      expect(TakSessionState.authenticated, isNotNull);")
a("      expect(TakSessionState.closed, isNotNull);")
a("    });")
a("")
a("    test('TakSessionEvent enum values', () {")
a("      expect(TakSessionEvent.values, hasLength(2));")
a("      expect(TakSessionEvent.authenticated, isNotNull);")
a("      expect(TakSessionEvent.disconnected, isNotNull);")
a("    });")
a("")
a("    test('TakServerEvent subclasses are sealed', () {")
a("      const connected = TakServerError('port in use');")
a("      expect(connected, isA<TakServerEvent>());")
a("      expect(connected.message, 'port in use');")
a("    });")
a("  });")
a("}")

with open(OUT, "w") as f:
    f.write("\n".join(lines) + "\n")
print(f"OK: {len(lines)} lines -> {OUT}")
