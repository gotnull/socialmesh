#!/usr/bin/env python3
"""Generate tak_bridge_providers_test.dart."""
import os

OUT = "/Users/fulvio/development/socialmesh/test/services/tak/tak_bridge_providers_test.dart"
os.makedirs(os.path.dirname(OUT), exist_ok=True)
lines = []
a = lines.append

a("// SPDX-License-Identifier: GPL-3.0-or-later")
a("// SPDX-FileCopyrightText: 2025-2026 gotnull (developer@socialmesh.app)")
a("import 'package:flutter_riverpod/flutter_riverpod.dart';")
a("import 'package:flutter_test/flutter_test.dart';")
a("import 'package:socialmesh/services/tak/providers/tak_bridge_providers.dart';")
a("")
a("void main() {")
a("  group('TakBridgeProviders', () {")
a("    test('isTakMeshBridgeEnabledProvider defaults to false', () {")
a("      final container = ProviderContainer();")
a("      addTearDown(container.dispose);")
a("")
a("      // Without .env setup, feature flag defaults to false.")
a("      final enabled = container.read(isTakMeshBridgeEnabledProvider);")
a("      expect(enabled, isFalse);")
a("    });")
a("")
a("    test('takCertManagerProvider creates instance', () {")
a("      final container = ProviderContainer();")
a("      addTearDown(container.dispose);")
a("")
a("      final manager = container.read(takCertManagerProvider);")
a("      expect(manager, isNotNull);")
a("    });")
a("")
a("    test('takIdentityRegistryProvider creates instance', () {")
a("      final container = ProviderContainer();")
a("      addTearDown(container.dispose);")
a("")
a("      final registry = container.read(takIdentityRegistryProvider);")
a("      expect(registry, isNotNull);")
a("    });")
a("")
a("    test('takServerProvider creates server', () {")
a("      final container = ProviderContainer();")
a("      addTearDown(container.dispose);")
a("")
a("      final server = container.read(takServerProvider);")
a("      expect(server, isNotNull);")
a("      expect(server.isRunning, isFalse);")
a("    });")
a("")
a("    test('takMeshBridgeProvider creates bridge', () {")
a("      final container = ProviderContainer();")
a("      addTearDown(container.dispose);")
a("")
a("      final bridge = container.read(takMeshBridgeProvider);")
a("      expect(bridge, isNotNull);")
a("      expect(bridge.isRunning, isFalse);")
a("    });")
a("")
a("    test('takBridgeStatusProvider returns initial status', () {")
a("      final container = ProviderContainer();")
a("      addTearDown(container.dispose);")
a("")
a("      final status = container.read(takBridgeStatusProvider);")
a("      expect(status.isRunning, isFalse);")
a("      expect(status.connectedClientCount, 0);")
a("      expect(status.packetsInbound, 0);")
a("      expect(status.packetsOutbound, 0);")
a("    });")
a("")
a("    test('TakBridgeStatus default constructor', () {")
a("      const status = TakBridgeStatus();")
a("      expect(status.isRunning, isFalse);")
a("      expect(status.connectedClientCount, 0);")
a("      expect(status.packetsInbound, 0);")
a("      expect(status.packetsOutbound, 0);")
a("      expect(status.lastActivity, isNull);")
a("    });")
a("  });")
a("}")

with open(OUT, "w") as f:
    f.write("\n".join(lines) + "\n")
print(f"OK: {len(lines)} lines -> {OUT}")
