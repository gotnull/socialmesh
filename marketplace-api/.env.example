# Firebase Authentication
# Download from Firebase Console > Project Settings > Service accounts > Generate new private key
# Option 1: Paste the entire JSON content (escape newlines)
FIREBASE_SERVICE_ACCOUNT_JSON=

# Option 2: Path to service account file (if mounted in Docker)
# GOOGLE_APPLICATION_CREDENTIALS=/app/firebase-service-account.json

# Admin users who can approve/reject marketplace widgets (comma-separated emails)
ADMIN_EMAILS=fcusumano@gmail.com

# Firebase Project ID (for reference)
FIREBASE_PROJECT_ID=social-mesh-app
